浏览器的**阅读模式（Reader Mode / 阅读视图）**本质上是一个**内容抽取（content extraction）与重排**的过程，并不是简单靠固定标签或人工规则，而是结合了一套**启发式规则 + DOM 分析算法**。下面从原理、常见规则、以及实际限制三个层面说明。

---

## 一、总体原理（高层视角）

阅读模式通常遵循以下流程：

1. **解析页面 DOM 树**
2. **识别“主要内容区域”**
3. **剔除干扰元素**（导航、广告、侧边栏、评论等）
4. **重排与样式统一**
5. **保留语义化信息**（标题、段落、图片、列表）

不同浏览器实现不同，但**核心思想高度一致**。

---

## 二、核心筛选规则（启发式为主）

以下是主流阅读模式（Safari Reader、Firefox Reader、Chrome Read Mode 背后的算法）的通用判断逻辑。

### 1. 文本密度（Text Density）

这是最重要的指标之一。

* 统计某个 DOM 节点中：

  * **纯文本长度**
  * 与之相比的：

    * 链接数量
    * 图片数量
    * 表单元素
* 规则倾向：

  * **长段连续文本**
  * **少链接、少交互控件**

例如：

```text
<div>
  <p>500 字正文</p>
</div>
```

优先级远高于：

```text
<ul>
  <li><a>link</a></li> × 20
</ul>
```

---

### 2. 标签语义权重（Semantic Weight）

HTML5 的语义标签会被**加权**：

| 标签          | 权重          |
| ----------- | ----------- |
| `<article>` | 极高          |
| `<main>`    | 很高          |
| `<section>` | 中           |
| `<p>`       | 高           |
| `<div>`     | 低（除非符合其他条件） |

如果页面结构合理，阅读模式几乎“秒识别”。

---

### 3. 类名 / ID 关键词匹配

浏览器会扫描 class / id 中的关键词：

**正向关键词（加分）**

* `article`
* `content`
* `post`
* `entry`
* `main`
* `body`

**负向关键词（减分或直接排除）**

* `nav`
* `menu`
* `footer`
* `header`
* `sidebar`
* `comment`
* `ad`
* `promo`

这也是为什么很多 CMS 默认命名能被很好识别。

---

### 4. 链接密度（Link Density）

阅读模式会计算：

```
链接文本长度 / 总文本长度
```

* 比例高 → 更像导航 / 推荐区
* 比例低 → 更像正文

典型被排除区域：

* 顶部导航
* “相关阅读”
* 标签云

---

### 5. DOM 位置与结构连续性

正文通常具备以下特征：

* 位于页面中部
* 垂直结构连续
* 不频繁被 `<hr>`、广告节点打断
* 子节点类型稳定（大量 `<p>`）

阅读模式会对 **“结构稳定性”**打分。

---

### 6. 图片与媒体处理规则

* 正文中的 `<img>` 会保留
* 轮播图、推荐图块常被移除
* `figure + figcaption` 组合权重较高

---

## 三、不同浏览器的实现差异

### Safari / WebKit

* 基于 **Readability 算法（早期）+ Apple 自己的优化**
* 对语义标签非常友好
* 对中文内容识别准确度较高

### Firefox

* 使用 **Mozilla Readability.js**
* 开源，可被很多第三方工具复用
* 对技术博客、Markdown 渲染页面效果很好

### Chrome

* 早期依赖简化版 Readability
* 新版 Read Mode 与 **AI 辅助结构判断**结合（但仍以规则为主）

---

## 四、它不是“严格规则”，而是打分模型

关键点：
**阅读模式不是 if-else 规则集合，而是多维度评分系统**。

简化表达：

```text
Score(node) =
  文本长度
+ 语义权重
+  结构连续性
-  链接密度
-  噪声关键词
```

得分最高的节点 → 视为正文容器。

---

## 五、为什么有时识别失败？

常见原因包括：

1. **大量内容由 JS 动态注入**
2. **页面结构扁平，全是 div**
3. **正文被拆得过碎**
4. **广告与正文混排**
5. **非文章型页面**（如文档工具、论坛流）

---

